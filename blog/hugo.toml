baseURL = "/"
languageCode = "en-us"
title = "Generals Research Notes"
disableKinds = ["taxonomy", "term", "RSS", "sitemap"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    noClasses = false
    style = "github-dark"

[params]
description = "Visual field notes from building learning agents for generals.io."
